# This include file only defines which dependencies we need, globally
set(torch_DEPENDENCIES cblas fftw3 imagemagick boost python blitz hdf)
set(torch_OPTIONALS googlePerfTools ffmpeg matio qt4 vlfeat)
