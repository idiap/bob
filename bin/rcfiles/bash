#/bin/bash
# This file complements the torch setup and has to used like this:
# *torch*: --rcfile %(thisfile)s
[ -e ~/.bashrc ] && source ~/.bashrc
PS1="(torch5spro @${TORCH_VERSION} / ${TORCH_PLATFORM})\n${PS1}"
